#include <stdio.h>
main()
{
int n,index,arr1[100],arr2[100];
scanf("%d",&n);
for(index=0;index<n;index++)
  scanf("%d",&a[index]);
for(index=0;index<n;index++)
  arr2[index]=arr1[n-1-index];
for(index=0;index<n;index++)
  printf("%d",b[index]);
}
